# SolveWatch AI — Windows / Linux / Intel Mac requirements
# Use this instead of requirements.txt on non-Apple-Silicon systems.
#
# Install:  pip install -r requirements-windows.txt

# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-socketio>=5.10.0
websocket-client>=1.6.0
aiohttp>=3.9.0

# Audio processing
sounddevice>=0.4.6
numpy>=1.24.0
librosa>=0.10.0
soundfile>=0.12.0
webrtcvad>=2.0.10
onnxruntime>=1.16.0  # Silero VAD

# Speech-to-text — openai-whisper (cross-platform, CPU or CUDA)
# Downloads model files to %USERPROFILE%\.cache\whisper\ on Windows
openai-whisper>=20231117

# Optional: GPU acceleration on Windows (CUDA)
# Replace the lines below with the +cu118 builds if you have an NVIDIA GPU:
# torch>=2.0.0+cu118 --extra-index-url https://download.pytorch.org/whl/cu118
# torchaudio>=2.0.0+cu118 --extra-index-url https://download.pytorch.org/whl/cu118

# Speaker identification (SpeechBrain ECAPA-TDNN, CPU or CUDA)
torch>=2.0.0
torchaudio>=2.0.0
speechbrain>=1.0.0

# OpenAI Whisper API fallback
openai>=1.0.0

# Question detection
transformers>=4.35.0
sentencepiece>=0.1.99
scikit-learn>=1.3.0

# Telemetry — OpenTelemetry → Grafana Cloud (OTLP HTTP)
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp-proto-http>=1.25.0
psutil>=5.9.0
nvidia-ml-py>=12.0.0  # NVIDIA GPU metrics (no-op on systems without an NVIDIA card)

# Utilities
pydantic>=2.5.0
python-dotenv>=1.0.0
pynput>=1.7.6
