# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-socketio>=5.10.0
websocket-client>=1.6.0  # Required for WebSocket transport in Socket.IO
aiohttp>=3.9.0

# Audio processing
sounddevice>=0.4.6
numpy>=1.24.0
librosa>=0.10.0  # For audio resampling
soundfile>=0.12.0  # For saving audio to WAV files
webrtcvad>=2.0.10  # Voice Activity Detection

# Speech-to-text (MLX Whisper - optimized for Apple Silicon)
# Note: MLX requires Apple Silicon (M1/M2/M3) Mac
mlx>=0.19.0
mlx-whisper>=0.4.0

# Question detection (optional — used by question_extractor.py)
transformers>=4.35.0
sentencepiece>=0.1.99
scikit-learn>=1.3.0

# Utilities
pydantic>=2.5.0
python-dotenv>=1.0.0
pynput>=1.7.6  # Keyboard input handling for push-to-record
