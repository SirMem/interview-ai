# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-socketio>=5.10.0
websocket-client>=1.6.0  # Required for WebSocket transport in Socket.IO
aiohttp>=3.9.0

# Audio processing
sounddevice>=0.4.6
numpy>=1.24.0
librosa>=0.10.0  # For audio resampling
soundfile>=0.12.0  # For saving audio to WAV files
webrtcvad>=2.0.10  # Voice Activity Detection (WebRTC engine)
onnxruntime>=1.16.0  # Silero VAD inference (DNN engine)

# Speech-to-text — MLX Whisper (Apple Silicon M1/M2/M3/M4 only)
# On Windows/Linux, use requirements-windows.txt instead.
mlx>=0.19.0
mlx-whisper>=0.4.0

# OpenAI Whisper API (used when stt_model = "whisper-1")
openai>=1.0.0

# Question detection (optional — used by question_extractor.py)
transformers>=4.35.0
sentencepiece>=0.1.99
scikit-learn>=1.3.0

# Speaker identification (SpeechBrain ECAPA-TDNN) — no HF token required
# https://huggingface.co/speechbrain/spkrec-ecapa-voxceleb
speechbrain>=1.0.0
torch>=2.0.0
torchaudio>=2.0.0

# Telemetry — OpenTelemetry → Grafana Cloud (OTLP HTTP)
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp-proto-http>=1.25.0
psutil>=5.9.0
# Optional: NVIDIA GPU metrics. Safe to keep installed on macOS — import is guarded.
nvidia-ml-py>=12.0.0; sys_platform != "darwin"

# Deepgram cloud STT (temporary integration — deepgram_enabled flag in config)
deepgram-sdk>=6.0.0

# Utilities
pydantic>=2.5.0
python-dotenv>=1.0.0
pynput>=1.7.6  # Keyboard input handling for push-to-record
